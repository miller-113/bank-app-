export type TFeatureCardProp = {
  icon: string;
  title: string;
  content: string;
  index: number | string;
};

export type TButtonProp = {
  styles?: string;

};

export type TFeedbackCardProp = {
  content: string;
  name: string;
  title: string;
  img: string;
};
